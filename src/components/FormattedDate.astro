---
import { getLocale, getLangFromUrl } from "../i18n/utils";
import { format } from "date-fns";
---

<time datetime={Astro.props.date}>
  {
    format(new Date(Astro.props.date), "P", {
      locale: getLocale(getLangFromUrl(Astro.url)),
    })
  }
</time>
