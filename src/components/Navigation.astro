---
import { Image } from "astro:assets";
import CTreffLogo from "../assets/ctreffos-logo.png";
import { getLangFromUrl } from "../i18n/utils";
import { Navigation as Nav } from "@/components/Navigation.tsx";

const lang = getLangFromUrl(Astro.url);
---

<div class="relative">
  {
    lang === "de" && (
      <a class="absolute top-3 right-3" href={`${import.meta.env.BASE_URL}/en`}>
        🇬🇧
      </a>
    )
  }
  {
    lang === "en" && (
      <a class="absolute top-3 right-3" href={`${import.meta.env.BASE_URL}/de`}>
        🇩🇪
      </a>
    )
  }
</div>
<a href={`${import.meta.env.BASE_URL}/${lang}`}>
  <header class="flex flex-col items-center">
    <Image
      src={CTreffLogo}
      alt="Chaostreff Osnabrück Logo"
      class="invert-0 dark:invert"
      width={517}
      height={299}
      loading="eager"
    />
  </header>
</a>

<Nav lang={lang} />
